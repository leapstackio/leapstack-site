---
const services = [
  {
    id: "01",
    title: "Implementation",
    desc: "Full-cycle Netsuite deployment tailored to your business model. We ensure a seamless transition with minimal downtime."
  },
  {
    id: "02",
    title: "Optimization",
    desc: "Refining existing workflows to maximize ROI. We audit, analyze, and enhance your system for peak performance."
  },
  {
    id: "03",
    title: "Integration",
    desc: "Connecting Netsuite with your entire tech stack. CRM, eCommerce, and 3PL integrations that flow data in real-time."
  },
  {
    id: "04",
    title: "Custom Scripting",
    desc: "Extending native functionality with robust SuiteScript solutions. When out-of-the-box isn't enough, we build it."
  }
];
---

<section id="services" class="services">
    <div class="container">
        <div class="section-header">
            <span class="font-tech text-gold">OUR EXPERTISE</span>
            <h2>Precision Engineering</h2>
        </div>

        <div class="services-grid">
            {services.map(service => (
                <div class="service-card">
                    <div class="card-header">
                        <span class="font-tech service-id">{service.id}</span>
                        <div class="corner-marker"></div>
                    </div>
                    <h3>{service.title}</h3>
                    <p>{service.desc}</p>
                    <a href="#contact" class="service-link">Learn More</a>
                </div>
            ))}
        </div>
    </div>
</section>

<style>
    .services {
        padding: var(--space-xl) 0;
        position: relative;
    }

    .section-header {
        margin-bottom: var(--space-lg);
        border-left: 1px solid var(--color-gold);
        padding-left: var(--space-sm);
    }

    .services-grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: 1px;
        background: var(--color-steel); /* Creates the borders */
        border: 1px solid var(--color-steel);
    }

    @media (min-width: 768px) {
        .services-grid {
            grid-template-columns: repeat(2, 1fr);
        }
    }

    .service-card {
        background: var(--color-void);
        padding: var(--space-md);
        position: relative;
        transition: background 0.3s ease;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        min-height: 300px;
    }

    .service-card:hover {
        background: #0a0a0a;
    }

    .card-header {
        display: flex;
        justify-content: space-between;
        margin-bottom: var(--space-md);
    }

    .service-id {
        color: var(--color-steel);
        font-size: 1.5rem;
    }

    .corner-marker {
        width: 10px;
        height: 10px;
        border-top: 1px solid var(--color-gold);
        border-right: 1px solid var(--color-gold);
        opacity: 0;
        transition: opacity 0.3s ease;
    }

    .service-card:hover .corner-marker {
        opacity: 1;
    }

    .service-card h3 {
        margin-bottom: var(--space-sm);
        color: #fff;
    }

    .service-card p {
        opacity: 0.7;
        margin-bottom: var(--space-md);
        flex-grow: 1;
    }

    .service-link {
        font-family: var(--font-tech);
        font-size: 0.8rem;
        text-transform: uppercase;
        color: var(--color-gold);
        letter-spacing: 0.05em;
        display: inline-block;
        transform: translateX(0);
        transition: transform 0.3s ease;
    }

    .service-card:hover .service-link {
        transform: translateX(5px);
    }
</style>
